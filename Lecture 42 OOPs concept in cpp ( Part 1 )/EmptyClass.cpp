#include<bits/stdc++.h>
using namespace std;

class hero {
    // No Properties
};

int main()
{
    // Object creation
    hero h1;

    cout << "size of h1: " << sizeof(h1) << endl; 
    /* In the case of the empty class (where there is no properties) 
    the pc gives it a 1 bit storage for its identification and tracking */
}